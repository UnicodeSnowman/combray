<section class="content-inner">
  <div class="row-fluid">
    <div>
      <ul class="thumbnails">
      <% @items.each do |item| %>
        <li >
          <div class="item thumbnail">
            <% if item.photos.empty? %>
              <%= image_tag('http://placehold.it/260x260') %>
            <% else %>
              <%= image_tag(item.photos.first.photo.thumb.url) %>
            <% end %>
    
            <%= link_to(item) do %>
              <div class="item-blurb">
                <p>
                  <%= item.name %>
                </p>
              </div>
            <% end %>
          </div>
        </li>
      <% end %>
      </ul> 
    </div>
  </div>
  <div class="row-fluid">
    <p class="">Displaying <%= @items.length %> item(s)</p>
  </div>
</section>

